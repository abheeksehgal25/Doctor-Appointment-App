# Backend environment variables (DO NOT COMMIT actual secrets)
# Copy this file to `backend/.env` and fill values before running in development or production.

# MongoDB connection string (without trailing slash or DB name). The app will append /appointy
# Example: mongodb+srv://username:password@cluster0.nozc9is.mongodb.net
MONGODB_URI=

# JWT secret used to sign tokens
JWT_SECRET=

# Cloudinary credentials (used for image uploads)
CLOUDINARY_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_SECRET_KEY=

# (Razorpay removed) If you add a payment provider later, document its server keys here.

# Currency (used for payments)
CURRENCY=INR

# Admin credentials for initial admin login (change in production)
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=changeme

# Port the backend listens on (default 4000)
PORT=4000

# Notes:
# - Do NOT commit your real `backend/.env` to git. Use platform secret management (Vercel/Heroku/Render/AWS).
# - If you prefer to include DB name in the connection string, modify `backend/config/mongodb.js` to not append `/appointy`.
